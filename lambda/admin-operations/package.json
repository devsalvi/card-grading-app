{
  "name": "card-grading-admin-operations",
  "version": "1.0.0",
  "description": "Lambda function for admin operations on card grading submissions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0"
  }
}
